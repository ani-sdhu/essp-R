% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/turnover.R
\name{analyze.additions}
\alias{analyze.additions}
\title{Capacity additions by year}
\usage{
analyze.additions(
  state = "US",
  year = NULL,
  years = NULL,
  month = 12,
  status = "OP",
  solar = c("utility", "total"),
  groups = NULL
)
}
\arguments{
\item{state}{Two-letter state code, or \code{"US"} for the national fleet
(default). Note the inventory has no \code{"US"} facet value -- a national
figure is the sum over all states, which is what omitting the filter does.}

\item{year}{Calendar year.}

\item{years}{Years of interest. Defaults to the ten years ending at \code{year}.}

\item{month}{Month of the snapshot, \code{1}-\code{12}. Defaults to December, or the
latest month available if the year is incomplete.}

\item{status}{Operating status codes to include. Defaults to \code{"OP"}
(operating), which excludes planned, standby, and retired units.}

\item{solar}{Passed to \code{\link[=essp.fuelgroups]{essp.fuelgroups()}}.}

\item{groups}{Resource groupings; defaults to \code{\link[=essp.fuelgroups]{essp.fuelgroups()}}.}
}
\value{
A tibble of \code{year}, \code{resource}, \code{mw}, and \code{generators}.
}
\description{
Nameplate capacity brought online each year, by resource, read from the
operating dates in the current inventory.
}
\details{
This is a survivorship view: it counts units that are \emph{still operating} at
the snapshot date, so historical years understate what was actually built --
anything since retired has vanished from the inventory. Recent years are
reliable; distant ones are a floor.
}
\examples{
\dontrun{
analyze.additions("US", 2024, years = 2015:2024)
}

}
