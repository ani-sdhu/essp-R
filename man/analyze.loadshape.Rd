% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load.R
\name{analyze.loadshape}
\alias{analyze.loadshape}
\title{Average daily load shape}
\usage{
analyze.loadshape(
  ba,
  start,
  end = NULL,
  by = c("none", "month", "season"),
  local = TRUE
)
}
\arguments{
\item{ba}{Balancing authority code, e.g. \code{"CISO"}. See \code{\link[=essp.respondents]{essp.respondents()}}.}

\item{start, end}{Period bounds, \code{"YYYY-MM-DDTHH"}. A UTC offset is appended
automatically when \code{local = TRUE}.}

\item{by}{Optional grouping: \code{"month"} or \code{"season"}.}

\item{local}{Use the respondent's local time (default) rather than UTC.
Local time is what makes hour-of-day meaningful.}
}
\value{
A tibble of \code{hour}, mean/min/max demand, and the grouping column.
}
\description{
Mean demand by hour of day, optionally split by month or season -- the
aggregation a demand-curve figure consumes.
}
\details{
Hours are local to the respondent, which is what makes an evening peak land
in the evening. Under UTC the same data smears the peak across the day.
}
\examples{
\dontrun{
analyze.loadshape("CISO", "2024-07-01T00", "2024-07-31T23")
}

}
