% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/output.R
\name{essp.analyze}
\alias{essp.analyze}
\title{Make gathered data analysis-ready}
\usage{
essp.analyze(
  data,
  value = NULL,
  by = NULL,
  derive = character(0),
  drop_units = TRUE
)
}
\arguments{
\item{data}{A tibble from \code{\link[=essp.gather]{essp.gather()}}.}

\item{value}{Measurement column to derive from. Defaults to the first one
found.}

\item{by}{Optional grouping column(s) for shares, e.g. \code{"fueltypeid"}.
Shares are computed within each period.}

\item{derive}{Any of \code{"share"} and \code{"growth"}. Defaults to neither.

\code{"share"} assumes the rows in each period are mutually exclusive. EIA fuel
data is not: a single response carries \code{ALL}, \code{FOS}, and \code{REN} alongside
the very codes they aggregate, so sharing it raw is meaningless. Filter to
a non-overlapping set first, or use \code{\link[=analyze.generation]{analyze.generation()}}, which selects
one code per resource and derives the residual.}

\item{drop_units}{Remove the \code{-units} companion columns, keeping the units
on the result as an attribute. Defaults to \code{TRUE}.}
}
\value{
A tibble with numeric measurement columns and a \code{year} column.
}
\description{
Takes raw \code{\link[=essp.gather]{essp.gather()}} output and does the cleanup every analysis
otherwise repeats: coerces measurement columns from character to numeric,
derives a \code{year} column from the period stamp, and optionally adds shares
and growth rates.
}
\details{
Measurement columns are found via their \code{-units} companions rather than by
name, so this works across routes whose columns differ.
}
\examples{
\dontrun{
essp.gather("retail", state = "GA", years = 2020:2024, freq = "annual") |>
  essp.analyze(value = "price", derive = "growth")
}

}
