% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/charts-general.R
\name{chart.timeseries}
\alias{chart.timeseries}
\title{Multi-series line chart}
\usage{
chart.timeseries(
  data,
  x,
  y,
  group = NULL,
  highlight = NULL,
  direct_label = TRUE,
  accent = NULL,
  semester = "Fall",
  year = as.integer(format(Sys.Date(), "\%Y"))
)
}
\arguments{
\item{data}{A data frame.}

\item{x, y}{Column names, as strings.}

\item{group}{Optional grouping column for multiple series.}

\item{highlight}{Value of \code{group} to emphasise.}

\item{direct_label}{Label each line at its end instead of using a legend.}

\item{accent, semester, year}{As in \code{\link[=chart.fleetmakeup]{chart.fleetmakeup()}}.}
}
\value{
A ggplot object.
}
\description{
One line per group, with the series under discussion in the accent color and
the rest greyed. Series are labelled at their right-hand end rather than in a
legend, so the eye never has to travel between a key and the lines.
}
\examples{
\dontrun{
analyze.prices(c("GA","OH"), 2010:2024) |>
  chart.timeseries("year", "price_real", group = "state", highlight = "GA")
}

}
