% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cite.R
\name{essp.cite}
\alias{essp.cite}
\title{Build a bibliography entry for gathered data}
\usage{
essp.cite(data, key = NULL, type = c("online", "dataset"), file = NULL)
}
\arguments{
\item{data}{A tibble returned by \code{\link[=essp.gather]{essp.gather()}}.}

\item{key}{Citation key. Defaults to one derived from the topic and the year
the data was retrieved, e.g. \code{eia_retail_2026}.}

\item{type}{Entry type. \code{"online"} (default) is what the existing briefs use
for EIA web sources; \code{"dataset"} is more precise but needs a biblatex
version that supports it.}

\item{file}{Optional path to append the entry to, typically a
\code{references.bib}. The entry is not appended if its key is already present.}
}
\value{
The entry as a single string, invisibly when \code{file} is given.
}
\description{
Turns the provenance recorded by \code{\link[=essp.gather]{essp.gather()}} into a biblatex entry ready
to paste into a brief's \code{references.bib}, capturing the route, the EIA
form(s) behind it, the filters applied, and the date retrieved.
}
\examples{
\dontrun{
x <- essp.gather("retail", state = "GA", years = 2023, freq = "annual")
cat(essp.cite(x))
essp.cite(x, file = "references.bib")
}

}
