% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/charts-demand.R
\name{chart.demandcurve}
\alias{chart.demandcurve}
\title{Daily demand curve, stacked by fuel}
\usage{
chart.demandcurve(
  data,
  highlight = NULL,
  order = NULL,
  bands = NULL,
  reserve_margin = NULL,
  mark_peak = FALSE,
  smooth = TRUE,
  palette = c("highlight", "house"),
  bold = highlight,
  italic = NULL,
  accent = NULL,
  label_min = 4,
  semester = "Fall",
  storage_ba = NULL,
  year = as.integer(format(Sys.Date(), "\%Y"))
)
}
\arguments{
\item{data}{Output of \code{\link[=analyze.fuelshape]{analyze.fuelshape()}}, or any tibble with \code{hour},
\code{fueltype}, and \code{mean_mw}.}

\item{highlight}{Fuel code or name to emphasise, e.g. \code{"NG"}.}

\item{order}{Fuel codes bottom-to-top. Defaults to a dispatch-like order;
anything not listed is appended.}

\item{bands}{Named numeric vector of threshold levels in MW, e.g.
\code{c(Baseload = 22500, Intermediate = 38000)}. Drawn as dotted lines with
labelled brackets down the right-hand side.}

\item{reserve_margin}{Length-2 numeric giving the lower and upper bounds of a
shaded reserve band, e.g. \code{c(44000, 48000)}.}

\item{mark_peak}{Mark the peak of the stack with a dot and a leader.}

\item{smooth}{Spline-smooth the bands. \code{FALSE} draws the raw hourly steps.}

\item{palette}{\code{"house"} (default) gives every fuel its own established
color, matching the concept-brief figures. \code{"highlight"} uses flat greys
behind a single accent, matching the resource-brief figures.

The two compose with \code{highlight}: under \code{"house"} the named fuel keeps
full saturation while the others are muted but keep their hues, so the
colour coding survives; under \code{"highlight"} the others go grey.}

\item{bold}{Fuel codes whose labels are drawn bold.}

\item{italic}{Fuel codes whose labels are drawn italic.}

\item{accent, semester, year}{As in \code{\link[=chart.fleetmakeup]{chart.fleetmakeup()}}.}

\item{label_min}{Smallest share of peak, in percent, that still gets a label.}
}
\value{
A ggplot object.
}
\description{
Average generation by hour of day, stacked bottom-to-top in dispatch order,
with the resource under discussion highlighted and every band labelled.
}
\details{
Bands are ordered baseload-first so the stack reads the way the grid
dispatches: nuclear and coal at the bottom running flat, gas filling the
middle, and peaking resources riding the evening ramp on top.

The optional annotations reproduce the house demand-curve figure: dotted
threshold lines with labelled brackets, a shaded reserve margin, and a marked
peak.
}
\examples{
\dontrun{
analyze.fuelshape("CISO", "2024-07-01T00", "2024-07-31T23") |>
  chart.demandcurve(
    highlight      = "NG",
    bands          = c(Baseload = 22500, Intermediate = 38000),
    reserve_margin = c(44000, 48000)
  )
}

}
