% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mix.R
\name{analyze.generation}
\alias{analyze.generation}
\title{Net generation by resource}
\usage{
analyze.generation(
  state = "US",
  year = NULL,
  sector = "99",
  solar = c("utility", "total"),
  groups = NULL
)
}
\arguments{
\item{state}{Two-letter state code, or \code{"US"} (default).}

\item{year}{Calendar year.}

\item{sector}{EIA sector id. Defaults to \code{"99"} (all sectors).}

\item{solar}{\code{"utility"} (default) counts only utility-scale solar (\code{SUN}),
matching the coverage of the Form 860 capacity inventory. \code{"total"} uses
\code{TSN}, which adds estimated small-scale distributed PV -- larger, but no
longer comparable like-for-like against capacity.}

\item{groups}{Resource groupings; defaults to \code{\link[=essp.fuelgroups]{essp.fuelgroups()}}.}
}
\value{
A tibble of \code{resource}, \code{mwh} (thousand megawatthours), and \code{share}.
}
\description{
Aggregates Form 923 operational data to resource-level generation.
}
\details{
EIA's fuel codes mix aggregates with their own components, so this selects
one non-overlapping code per resource and derives \code{"Other"} as the residual
against the \code{ALL} total rather than summing leftovers -- adding codes
indiscriminately double-counts heavily.
}
\examples{
\dontrun{
analyze.generation("US", 2024)
}

}
