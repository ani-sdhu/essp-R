% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stack.R
\name{essp_stack}
\alias{essp_stack}
\title{Re-run a metric once per (where, year) and row-bind the results}
\usage{
essp_stack(mc, expand, where_arg = "state", env = parent.frame())
}
\arguments{
\item{mc}{The metric's own \code{match.call()}.}

\item{expand}{The list from \code{\link[=essp_expand]{essp_expand()}}.}

\item{where_arg}{The metric's place argument name (\code{"state"} or \code{"fuel"}).}

\item{env}{The caller environment the other arguments must evaluate in.}
}
\value{
One stacked tibble.
}
\description{
Rewrites the captured call with each scalar \code{where}/\code{year} in turn, evaluates
it in the caller's environment (so the metric's body runs on a single
combination), tags the result with \code{where}/\code{year} columns, and stacks.
}
\keyword{internal}
