% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/storagedisplace.R
\name{analyze.storagedisplace}
\alias{analyze.storagedisplace}
\title{Insert battery discharge into the dispatch stack}
\usage{
analyze.storagedisplace(data, storage, displaces = c("NGCT", "NGCC", "NG"))
}
\arguments{
\item{data}{Hourly fuel shape, as from \code{\link[=analyze.fuelshape]{analyze.fuelshape()}}.}

\item{storage}{Output of \code{\link[=analyze.storage]{analyze.storage()}}, or any frame with \code{hour},
\code{charge_mw}, and \code{discharge_mw}.}

\item{displaces}{Fuel codes to displace, in order. Defaults to peakers then
combined cycle.}
}
\value{
\code{data} with a \code{Storage} band added and the displaced fuels reduced.
Charging is carried on the result as the \code{essp_charge} attribute, since it
sits below the axis rather than in the stack.
}
\description{
Adds a \code{Storage} band to an hourly fuel shape and subtracts an equal amount
from the plant it displaces, so total generation is unchanged.
}
\details{
This is what makes the storage figure honest. Batteries do not add supply on
top of demand -- they substitute for whatever would otherwise have run.
Discharge therefore displaces the most expensive plant first (simple-cycle
peakers), then the next (combined cycle), leaving the demand envelope exactly
where it was. Stacking storage on top instead would draw a taller peak than
the grid actually served.
}
\examples{
\dontrun{
fs <- analyze.fuelshape("CISO", "2024-07-01T00", "2024-07-31T23")
st <- analyze.storage("CAL", "2024-07-01T00", "2024-07-31T23")
analyze.storagedisplace(fs, st)
}

}
