% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stack.R
\name{essp_when_window}
\alias{essp_when_window}
\title{Resolve a start/end window, allowing a single normative \code{when} string}
\usage{
essp_when_window(start, end = NULL)
}
\arguments{
\item{start}{A timestamp, or a normative \code{when} string when \code{end} is \code{NULL}.}

\item{end}{The upper bound, or \code{NULL} to expand \code{start} as a window.}
}
\value{
\code{list(start=, end=)}.
}
\description{
The hourly commands historically took \code{start} and \code{end} ISO timestamps. They
now also accept a single plain string in \code{start} (with \code{end} left \code{NULL}) --
\code{"July 2024"}, \code{"summer 2024"}, \code{"2024-07-31"}, \code{"2024"} -- which is expanded
via \code{\link[=essp_parse_when]{essp_parse_when()}}. Two explicit bounds are passed through unchanged.
}
\keyword{internal}
