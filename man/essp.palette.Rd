% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme.R
\name{essp.palette}
\alias{essp.palette}
\title{Chart palettes}
\usage{
essp.palette(type = c("categorical", "grey", "sequential"), n = 4)
}
\arguments{
\item{type}{\code{"categorical"}, \code{"grey"}, or \code{"sequential"}.}

\item{n}{Number of colors. Categorical is capped at 4 -- see the note below.}
}
\value{
A character vector of hex colors.
}
\description{
Chart palettes
}
\details{
Categorical is capped at \strong{four} hues because a fifth cannot be told apart
by protanopes (validated CVD separation 4.7, against a floor of 6). Charts
needing more categories should use \code{\link[=essp.highlight]{essp.highlight()}} instead, which greys
everything except the resource in question and relies on direct labels for
identity. Even at four, direct labels are required -- separation sits at 6.8,
inside the band that is legal only with secondary encoding.

The grey ramp is deliberately non-categorical. Its steps are close together
by design; they are backgrounded context, and identity comes from labels
rather than hue.
}
\examples{
essp.palette("categorical", 4)
essp.palette("grey", 5)

}
