% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/topics.R
\name{essp.topics}
\alias{essp.topics}
\title{List the topics ESSP can gather}
\usage{
essp.topics(topic = NULL, verified_only = FALSE)
}
\arguments{
\item{topic}{Optional topic name to filter to. If \code{NULL} (default), all
topics are returned.}

\item{verified_only}{If \code{TRUE}, return only rows checked against the live
API. Defaults to \code{FALSE}.}
}
\value{
A tibble with one row per topic.
}
\description{
Prints the topic vocabulary used by \code{\link[=essp.gather]{essp.gather()}}, showing the EIA route
and form(s) each topic resolves to, the route's own facet names, its
available frequencies, and its period coverage.
}
\details{
The EIA API is organized by route, not by form number -- form numbers appear
only in route descriptions, and one form's data is often split across several
routes. Nor is there a shared facet vocabulary: the geography facet is
spelled \code{stateid}, \code{stateId}, \code{location}, \code{state}, \code{respondent}, \code{duoarea},
or \code{countryRegionId} depending on the route. Neither mapping can be derived
from the API, so ESSP maintains both by hand and exposes them here rather
than burying them.
}
\examples{
essp.topics()
essp.topics("hourly")

}
