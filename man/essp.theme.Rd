% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme.R
\name{essp.theme}
\alias{essp.theme}
\title{House ggplot theme}
\usage{
essp.theme(
  semester = "Fall",
  year = as.integer(format(Sys.Date(), "\%Y")),
  base_size = 11,
  font_dir = system.file("fonts", package = "ESSP")
)
}
\arguments{
\item{semester}{\code{"Spring"}, \code{"Summer"}, or \code{"Fall"}.}

\item{year}{Calendar year.}

\item{base_size}{Base font size in points.}

\item{font_dir}{Directory holding the Merriweather static \code{.ttf} files.
Defaults to the copies bundled with the package; \code{NULL} skips font
registration.}
}
\value{
A ggplot2 theme object.
}
\description{
Typography, grid, and spacing shared by every ESSP figure. The accent for the
given semester is attached as an attribute so charts can reach it without a
second call.
}
\details{
Merriweather matches the briefs' body text and is used when the \code{showtext}
and \code{sysfonts} packages are available along with a font directory; otherwise
the device default applies and the chart still renders.
}
\examples{
\dontrun{
library(ggplot2)
ggplot(mtcars, aes(wt, mpg)) + geom_point() + essp.theme("Fall", 2026)
}

}
