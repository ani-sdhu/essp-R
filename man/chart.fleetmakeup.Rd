% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/charts.R
\name{chart.fleetmakeup}
\alias{chart.fleetmakeup}
\title{Capacity versus generation, as a 0-100\% stacked comparison}
\usage{
chart.fleetmakeup(
  data,
  highlight = NULL,
  accent = NULL,
  show_pct = TRUE,
  label_min = 3,
  palette = c("highlight", "house"),
  semester = "Fall",
  year = as.integer(format(Sys.Date(), "\%Y"))
)
}
\arguments{
\item{data}{Output of \code{\link[=analyze.mix]{analyze.mix()}}, or any tibble with \code{metric},
\code{resource}, and \code{share}.}

\item{highlight}{Resource to emphasise, e.g. \code{"Solar"}.}

\item{accent}{Accent color. Defaults to the theme's semester accent.}

\item{show_pct}{Print the percentage inside the highlighted segments.}

\item{label_min}{Smallest share, in percent, that still gets a label. Below
this the text will not fit and is dropped.}

\item{semester, year}{Passed to \code{\link[=essp.theme]{essp.theme()}}.}
}
\value{
A ggplot object.
}
\description{
The signature figure: two bars, one for capacity and one for generation, each
summing to 100\%, with a single resource highlighted. The gap between a
resource's two segments is the point -- solar and wind occupy more of the
capacity bar than the generation bar because their capacity factors are low,
while nuclear does the reverse.
}
\details{
Every segment is labelled directly. That is a requirement rather than a
flourish: the non-highlighted resources are distinguished only by grey steps,
which are close together on purpose, so labels carry identity instead of
color.
}
\examples{
\dontrun{
analyze.mix("US", 2024) |> chart.fleetmakeup(highlight = "Solar")
}

}
