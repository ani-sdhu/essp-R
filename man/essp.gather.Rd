% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gather.R
\name{essp.gather}
\alias{essp.gather}
\title{Gather energy data from the EIA API}
\usage{
essp.gather(
  topic,
  state = NULL,
  years = NULL,
  freq = NULL,
  ba = NULL,
  fuel = NULL,
  sector = NULL,
  columns = NULL,
  facets = NULL,
  start = NULL,
  end = NULL,
  max_rows = 250000,
  throttle = 0.3,
  cache = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{topic}{A topic name (\code{"generation"}), an EIA form number (\code{"923"}), or
a raw route path (\code{"electricity/retail-sales"}) for datasets the topic map
does not cover. See \code{\link[=essp.topics]{essp.topics()}}.}

\item{state}{State or region code(s), e.g. \code{"GA"} or \code{c("GA", "OH")}. Mapped
to whichever facet the route uses for geography.}

\item{years}{Calendar years to fetch, e.g. \code{2023} or \code{2019:2024}. Converted
to the route's own period format.}

\item{freq}{Frequency id such as \code{"monthly"} or \code{"annual"}. Defaults to the
route's own default.}

\item{ba}{Balancing authority code(s) for hourly routes, e.g. \code{"CISO"}.}

\item{fuel}{Fuel or energy source code(s).}

\item{sector}{Sector code(s), e.g. \code{"RES"}.}

\item{columns}{Measurement columns to request. Defaults to every column the
route offers, which is almost always what you want.}

\item{facets}{Additional facets as a named list, for anything without a
dedicated argument (e.g. \code{list(prime_mover_code = "CT")}).}

\item{start, end}{Explicit period bounds in the route's own format. Override
\code{years} when supplied.}

\item{max_rows}{Stop after roughly this many rows. Guards against
accidentally pulling millions.}

\item{throttle}{Seconds to pause between paged requests.}

\item{cache}{Passed to \code{\link[eia:eia_data]{eia::eia_data()}}.}

\item{verbose}{Report progress while paging.}
}
\value{
A tibble. Source metadata is attached as an \code{essp_provenance}
attribute; see \code{\link[=essp.provenance]{essp.provenance()}}. Its \code{total} element is best-effort:
EIA reports the full row count only inside a warning, which is not
re-emitted on a cache hit, so \code{total} is \code{NA} for cached queries.
}
\description{
The single entry point for data acquisition. Resolves a plain-English topic
to an EIA route, reads that route's metadata, requests every available
measurement column, translates \code{state}/\code{fuel}/\code{sector} into the facet names
that route actually uses, pages through results beyond the API's 5,000-row
cap, throttles between requests, and records where the data came from.
}
\examples{
\dontrun{
essp.gather("retail", state = "GA", years = 2023, freq = "annual")
essp.gather("hourly", ba = "CISO", years = 2024)
}

}
