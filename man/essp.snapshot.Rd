% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/snapshot.R
\name{essp.snapshot}
\alias{essp.snapshot}
\title{Read a frozen snapshot, or create it on first use}
\usage{
essp.snapshot(path, expr, refresh = FALSE)
}
\arguments{
\item{path}{Path to the CSV, typically inside the brief's own folder.}

\item{expr}{Expression producing the data, normally an \code{\link[=essp.gather]{essp.gather()}} call.
Not evaluated if the snapshot already exists.}

\item{refresh}{Force re-evaluation and overwrite the snapshot.}
}
\value{
A tibble. Provenance is restored from the sidecar when read back.
}
\description{
Returns the CSV at \code{path} if it exists. If it does not, \code{expr} is evaluated,
its result written to \code{path} with a provenance sidecar, and returned.
}
\details{
This is the form a brief should use. On the first render it fetches and
freezes; on every render after that it reads the committed CSV and makes no
network request at all. The figure in a published PDF then cannot drift when
EIA restates its data, and the numbers behind it stay open to inspection and
correction.

\code{expr} is only evaluated when the snapshot is missing, so a cached brief
needs no API key to render.
}
\examples{
\dontrun{
gen <- essp.snapshot(
  "data/us_generation_2024.csv",
  essp.gather("generation", state = "US", years = 2024, freq = "annual")
)
}

}
