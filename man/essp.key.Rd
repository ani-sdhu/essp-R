% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/key.R
\name{essp.key}
\alias{essp.key}
\title{Set or check your EIA API key}
\usage{
essp.key(key = NULL, persist = TRUE)
}
\arguments{
\item{key}{Your EIA API key (a ~40-character letters-and-digits string).
Omit to check the current status instead of setting one.}

\item{persist}{Also write the key to \verb{~/.Renviron} so later R sessions pick
it up automatically. \code{TRUE} by default; set \code{FALSE} to keep it to this
session only.}
}
\value{
Invisibly \code{TRUE} if a key is now set, \code{FALSE} otherwise. Called for
its side effect.
}
\description{
The gathering commands need a (free) EIA API key from
\url{https://www.eia.gov/opendata/}. \code{essp.key("your-key")} sets it for the
current session and, by default, saves it to \verb{~/.Renviron} so you only ever
do this once. \code{essp.key()} with no argument reports whether a key is set,
without ever printing it.
}
\examples{
\dontrun{
essp.key("abcdef0123456789abcdef0123456789abcdef01")
essp.key()   # is a key set?
}

}
