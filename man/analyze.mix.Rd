% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mix.R
\name{analyze.mix}
\alias{analyze.mix}
\title{Capacity and generation mix, aligned}
\usage{
analyze.mix(
  state = "US",
  year = NULL,
  month = 12,
  basis = c("nameplate", "summer", "winter"),
  status = "OP",
  sector = "99",
  solar = c("utility", "total"),
  groups = NULL
)
}
\arguments{
\item{state}{Two-letter state code, or \code{"US"} for the national fleet
(default). Note the inventory has no \code{"US"} facet value -- a national
figure is the sum over all states, which is what omitting the filter does.}

\item{year}{Calendar year.}

\item{month}{Month of the snapshot, \code{1}-\code{12}. Defaults to December, or the
latest month available if the year is incomplete.}

\item{basis}{\code{"nameplate"} (default), \code{"summer"}, or \code{"winter"} capacity.}

\item{status}{Operating status codes to include. Defaults to \code{"OP"}
(operating), which excludes planned, standby, and retired units.}

\item{sector}{EIA sector id for the generation side. Defaults to \code{"99"}.}

\item{solar}{\code{"utility"} (default) keeps both metrics on utility-scale
coverage, so the comparison is like-for-like. \code{"total"} adds estimated
small-scale distributed PV to \emph{generation only} -- Form 860 does not
inventory sub-1 MW systems, so capacity cannot match it. That asymmetry
inflates solar's generation share relative to its capacity share, which
works against what the figure is meant to show; a warning is issued.}

\item{groups}{Resource groupings; defaults to \code{\link[=essp.fuelgroups]{essp.fuelgroups()}}.}
}
\value{
A tibble of \code{metric} (\code{"Capacity"}/\code{"Generation"}), \code{resource},
\code{value}, \code{units}, and \code{share}.
}
\description{
Combines \code{\link[=analyze.capacity]{analyze.capacity()}} and \code{\link[=analyze.generation]{analyze.generation()}} into the long
two-metric shape the fleet-makeup figure consumes: one row per resource per
metric, with each metric's shares summing to 100.
}
\details{
The contrast between the two columns is the point of the figure -- resources
with low capacity factors (solar, wind) occupy more of the capacity bar than
the generation bar, and baseload resources (nuclear) the reverse.
}
\examples{
\dontrun{
analyze.mix("US", 2024)
analyze.mix("US", 2024, solar = "total")   # reproduces the older figures
}

}
