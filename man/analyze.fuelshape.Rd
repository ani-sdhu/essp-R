% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load.R
\name{analyze.fuelshape}
\alias{analyze.fuelshape}
\title{Average daily generation shape by fuel}
\usage{
analyze.fuelshape(ba, start, end = NULL, fuels = NULL, local = TRUE)
}
\arguments{
\item{ba}{Balancing authority code, e.g. \code{"CISO"}. See \code{\link[=essp.respondents]{essp.respondents()}}.}

\item{start, end}{Period bounds, \code{"YYYY-MM-DDTHH"}. A UTC offset is appended
automatically when \code{local = TRUE}.}

\item{fuels}{Optional fuel codes to keep, e.g. \code{c("NG", "NUC", "SUN")}.
\code{NULL} keeps all.}

\item{local}{Use the respondent's local time (default) rather than UTC.
Local time is what makes hour-of-day meaningful.}
}
\value{
A tibble of \code{hour}, \code{fueltype}, \code{fuel_name}, \code{mean_mw}, and \code{n}.
}
\description{
Mean generation by hour of day and fuel type -- the stacked profile a demand
curve figure is built from.
}
\details{
Hours are local to the respondent. Under UTC the solar peak lands hours away
from midday and the whole shape becomes meaningless.
}
\examples{
\dontrun{
analyze.fuelshape("CISO", "2024-07-01T00", "2024-07-31T23")
}

}
