% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fleet.R
\name{analyze.fuelleaders}
\alias{analyze.fuelleaders}
\alias{analyze.topproducers}
\title{Rank states by production of a resource}
\usage{
analyze.fuelleaders(
  fuel,
  year = NULL,
  n = 10,
  sector = "99",
  solar = c("utility", "total"),
  include_pr = FALSE
)

analyze.topproducers(...)
}
\arguments{
\item{fuel}{A resource name as used by \code{\link[=essp.fuelgroups]{essp.fuelgroups()}} (e.g. \code{"Solar"}),
or a raw EIA \code{fueltypeid}.}

\item{year}{Calendar year.}

\item{n}{Number of states to return. \code{Inf} returns all.}

\item{sector}{EIA sector id. Defaults to \code{"99"} (all sectors).}

\item{solar}{Passed to \code{\link[=essp.fuelgroups]{essp.fuelgroups()}}.}

\item{include_pr}{Include Puerto Rico, which EIA reports alongside the
states but excludes from its US total. Defaults to \code{FALSE}.}
}
\value{
A tibble of \code{rank}, \code{state}, \code{mwh}, \code{share}, and \code{cumulative_share}.
}
\description{
Produces the "N states account for X\% of output" claim directly, with the
cumulative share carried alongside each rank so the sentence can be written
from the table rather than by hand.
}
\details{
The \code{location} facet mixes true states with census regions (\code{PCC} Pacific
Contiguous, \code{SAT} South Atlantic, ...), the \code{US} total, and Puerto Rico.
Ranking them together double-counts, since a region already contains its
member states -- cumulative share then runs past 100\%. Only the 50 states
and DC are ranked here.
}
\examples{
\dontrun{
analyze.fuelleaders("Solar", 2024, n = 8)
}

}
