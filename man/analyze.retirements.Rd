% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/turnover.R
\name{analyze.retirements}
\alias{analyze.retirements}
\title{Announced capacity retirements}
\usage{
analyze.retirements(
  state = "US",
  year = NULL,
  from = NULL,
  through = NULL,
  month = 12,
  status = "OP",
  solar = c("utility", "total"),
  groups = NULL
)
}
\arguments{
\item{state}{Two-letter state code, or \code{"US"} for the national fleet
(default). Note the inventory has no \code{"US"} facet value -- a national
figure is the sum over all states, which is what omitting the filter does.}

\item{year}{Calendar year.}

\item{from}{Earliest retirement year to include. Defaults to \code{year}, so only
retirements still ahead of the snapshot are counted.}

\item{through}{Latest retirement year to include. Defaults to ten years out.}

\item{month}{Month of the snapshot, \code{1}-\code{12}. Defaults to December, or the
latest month available if the year is incomplete.}

\item{status}{Operating status codes to include. Defaults to \code{"OP"}
(operating), which excludes planned, standby, and retired units.}

\item{solar}{Passed to \code{\link[=essp.fuelgroups]{essp.fuelgroups()}}.}

\item{groups}{Resource groupings; defaults to \code{\link[=essp.fuelgroups]{essp.fuelgroups()}}.}
}
\value{
A tibble of \code{year}, \code{resource}, \code{mw}, and \code{generators}.
}
\description{
Capacity with a planned retirement date on file, by year and resource.
}
\details{
These are only the retirements operators have \emph{announced} on Form 860. Units
with no planned date are excluded, so this is a floor on future retirements,
not a forecast.

A handful of still-operating units carry planned-retirement dates in the
distant past (some as early as the 1950s) -- stale entries rather than real
plans. Years before \code{from} are dropped for that reason; set \code{from} lower to
inspect them.
}
\examples{
\dontrun{
analyze.retirements("US", 2024)
}

}
